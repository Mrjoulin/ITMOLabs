# Модуль подсчёта людей

### Для установки зависимостей:`pip install -r requirements.txt`
### Для запуска `python wsgi.py`
### Также возможно указание порта через флаг `--port` и хоста через `--host`
